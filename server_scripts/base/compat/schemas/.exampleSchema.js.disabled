/**
 * @template {number} int
 * @template {number} float
 * @template {#&item} itemtag
 * @template {string} item
 * @template {string} fluid
 */

if (global.startup.mods.modname) {
  
  global.server.recipes.compat.modname.functionName = () => {
    ServerEvents.recipes((event) => {
      // code here
    });
  };

}
